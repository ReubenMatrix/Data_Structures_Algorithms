#include<iostream>
#include<vector>
using namespace std;

int solution(vector<int> arr){
    int maxSub = arr[0];
    int currSum = 0;

    for(int i = 0; i < arr.size(); i++){
        if(currSum < 0){
            
        }
    }
}